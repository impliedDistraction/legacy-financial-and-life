---
import { site } from '../content/site';
---
<section id="plans" class="mx-auto max-w-6xl px-4 py-12 sm:py-16">
  <h2 class="text-2xl sm:text-3xl font-bold">Our Coverage Options</h2>
  <p class="mt-2 text-slate-700">Protect what matters most with plans built around your life.</p>

  <div class="mt-8 grid gap-6 sm:grid-cols-2 lg:grid-cols-4">
    {site.features.map((f) => (
      <article class="rounded-2xl border border-slate-200 p-5 hover:shadow-sm">
        <h3 class="font-semibold">{f.title}</h3>
        <p class="mt-2 text-slate-700">{f.body}</p>
      </article>
    ))}
  </div>
</section>

<section id="retirement" class="mx-auto max-w-6xl px-4 py-12 sm:py-16">
  <h2 class="text-2xl sm:text-3xl font-bold">{site.retirement.title}</h2>
  <ul class="mt-4 grid gap-3">
    {site.retirement.points.map((p) => <li class="flex items-start gap-2"><span aria-hidden="true">✓</span><span>{p}</span></li>)}
  </ul>
</section>