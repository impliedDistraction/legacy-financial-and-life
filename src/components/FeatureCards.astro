---
import { site } from '../content/site';
---
<section id="plans" class="mx-auto max-w-6xl px-4 py-12 sm:py-16">
  <h2 class="text-2xl sm:text-3xl font-bold scroll-hidden">Our Coverage Options</h2>
  <p class="mt-2 text-slate-700 scroll-hidden" style="transition-delay: 0.1s;">Protect what matters most with plans built around your life.</p>

  <div class="mt-8 grid gap-6 sm:grid-cols-2 lg:grid-cols-4" data-animate-group="feature-cards">
    {site.features.map((f) => (
      <article class="rounded-2xl border border-slate-200 p-5 hover:shadow-sm hover-lift scroll-hidden">
        <h3 class="font-semibold">{f.title}</h3>
        <p class="mt-2 text-slate-700">{f.body}</p>
      </article>
    ))}
  </div>
</section>

<section id="retirement" class="mx-auto max-w-6xl px-4 py-12 sm:py-16">
  <h2 class="text-2xl sm:text-3xl font-bold scroll-hidden">{site.retirement.title}</h2>
  <ul class="mt-4 grid gap-3" data-animate-group="retirement-points">
    {site.retirement.points.map((p) => <li class="flex items-start gap-2 scroll-hidden"><span aria-hidden="true">✓</span><span>{p}</span></li>)}
  </ul>
</section>