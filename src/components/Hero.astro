---
import { site } from '../content/site';
---
<section class="relative isolate overflow-hidden">
  <div class="mx-auto max-w-6xl px-4 py-20 sm:py-28">
    <p class="text-brand-700 font-semibold">{site.tagline}</p>
    <h1 class="mt-2 text-4xl sm:text-5xl font-extrabold tracking-tight">{site.hero.heading}</h1>
    <p class="mt-4 max-w-2xl text-lg text-slate-700">{site.hero.sub}</p>
    <div class="mt-8 flex gap-3">
      <a href="#contact" class="rounded-lg bg-brand-600 hover:bg-brand-700 text-white px-5 py-3 font-semibold">
        {site.hero.cta}
      </a>
      <a href="#plans" class="rounded-lg border border-slate-300 px-5 py-3 font-semibold hover:border-slate-400">
        View Plans
      </a>
    </div>
  </div>
</section>