---
import { site } from '../content/site';
---
<section id="team" class="mx-auto max-w-6xl px-4 py-12 sm:py-16">
  <h2 class="text-2xl sm:text-3xl font-bold">{site.team.heading}</h2>
  <div class="mt-6 grid gap-8 sm:grid-cols-5 items-start">
    <img src="/og-image.jpg" alt={site.team.headshotAlt} class="rounded-2xl border border-slate-200 sm:col-span-2">
    <div class="sm:col-span-3">
      <p class="text-slate-700">{site.team.intro}</p>
      <ul class="mt-4 space-y-2">
        {site.team.bullets.map((b) => <li class="flex gap-2"><span aria-hidden="true">•</span><span>{b}</span></li>)}
      </ul>
    </div>
  </div>
</section>