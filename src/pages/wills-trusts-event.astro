---
import Base from '../layouts/Base.astro';
import Header from '../components/Header.astro';
import EventRSVP from '../components/EventRSVP.astro';
import Footer from '../components/Footer.astro';
import { site } from '../content/site';
import '../styles/tailwind.css';
---

<Base 
  title={`${site.event.title} Event — Legacy Financial & Life`}
  description={`${site.event.description} Join Tim and Beth Byrd for this free educational seminar on estate planning and protecting your family's future.`}
>
  <Header />
  
  <!-- Event Hero Section -->
  <section class="relative isolate overflow-hidden bg-gradient-to-br from-brand-50 to-white py-16 sm:py-20">
    <div class="absolute inset-x-0 -top-40 -z-10 transform-gpu overflow-hidden blur-3xl sm:-top-80" aria-hidden="true">
      <div class="relative left-[calc(50%-11rem)] aspect-[1155/678] w-[36.125rem] -translate-x-1/2 rotate-[30deg] bg-gradient-to-tr from-brand-200 to-brand-400 opacity-20 sm:left-[calc(50%-30rem)] sm:w-[72.1875rem]"></div>
    </div>
    
    <div class="mx-auto max-w-4xl px-4 text-center">
      <div class="animate-fade-in-up">
        <div class="inline-block bg-brand-100 text-brand-800 text-sm font-medium px-4 py-2 rounded-full mb-6">
          🎓 {site.event.subtitle}
        </div>
        <h1 class="text-4xl sm:text-5xl lg:text-6xl font-bold tracking-tight text-slate-900 mb-6">
          {site.event.title}
        </h1>
        <p class="text-xl text-slate-700 mb-8 max-w-2xl mx-auto">
          {site.event.description}
        </p>
        
        <!-- Event Quick Details -->
        <div class="bg-white rounded-2xl border border-slate-200 p-6 shadow-lg mb-8 max-w-2xl mx-auto">
          <div class="grid sm:grid-cols-3 gap-4 text-center">
            <div>
              <div class="text-brand-600 font-semibold text-sm uppercase tracking-wide">Date</div>
              <div class="text-slate-900 font-bold text-lg">{site.event.date}</div>
            </div>
            <div>
              <div class="text-brand-600 font-semibold text-sm uppercase tracking-wide">Time</div>
              <div class="text-slate-900 font-bold text-lg">{site.event.time}</div>
            </div>
            <div>
              <div class="text-brand-600 font-semibold text-sm uppercase tracking-wide">Location</div>
              <div class="text-slate-900 font-bold text-lg">{site.event.location}</div>
            </div>
          </div>
        </div>

        <!-- CTA Buttons -->
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a 
            href="#event-rsvp" 
            class="rounded-lg bg-brand-600 hover:bg-brand-700 text-white px-8 py-4 text-lg font-semibold btn-animate hover-lift"
          >
            {site.event.callToAction}
          </a>
          <a 
            href="#event-details" 
            class="rounded-lg border border-brand-600 text-brand-600 hover:bg-brand-50 px-8 py-4 text-lg font-semibold transition-colors"
          >
            Learn More
          </a>
        </div>
      </div>
    </div>
    
    <div class="absolute inset-x-0 top-[calc(100%-13rem)] -z-10 transform-gpu overflow-hidden blur-3xl sm:top-[calc(100%-30rem)]" aria-hidden="true">
      <div class="relative left-[calc(50%+3rem)] aspect-[1155/678] w-[36.125rem] -translate-x-1/2 bg-gradient-to-tr from-brand-200 to-brand-400 opacity-20 sm:w-[72.1875rem]"></div>
    </div>
  </section>

  <!-- Event Details Section -->
  <section id="event-details" class="mx-auto max-w-6xl px-4 py-12 sm:py-16">
    <div class="text-center mb-12">
      <h2 class="text-3xl sm:text-4xl font-bold mb-4">What You'll Learn</h2>
      <p class="text-xl text-slate-700 max-w-3xl mx-auto">
        This comprehensive seminar covers essential topics to help you protect your family's future and navigate estate planning decisions with confidence.
      </p>
    </div>

    <!-- Topics Grid -->
    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6 mb-12">
      {site.event.topics.map((topic) => (
        <div class="bg-white rounded-xl border border-slate-200 p-6 hover-lift">
          <div class="flex items-start space-x-3">
            <div class="flex-shrink-0 w-8 h-8 bg-brand-100 rounded-lg flex items-center justify-center">
              <svg class="w-4 h-4 text-brand-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
            </div>
            <h3 class="text-lg font-semibold text-slate-900 leading-tight">{topic}</h3>
          </div>
        </div>
      ))}
    </div>

    <!-- Why Attend Section -->
    <div class="bg-slate-50 rounded-2xl p-8 sm:p-12">
      <div class="text-center mb-8">
        <h3 class="text-2xl sm:text-3xl font-bold mb-4">Why Attend This Event?</h3>
        <p class="text-lg text-slate-700">
          Get expert insights from Tim & Beth Byrd and take control of your family's financial future.
        </p>
      </div>

      <div class="grid md:grid-cols-2 gap-6">
        {site.event.benefits.map((benefit) => (
          <div class="flex items-start space-x-3">
            <div class="flex-shrink-0 w-6 h-6 bg-brand-500 rounded-full flex items-center justify-center mt-0.5">
              <svg class="w-3 h-3 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7"></path>
              </svg>
            </div>
            <p class="text-slate-700 font-medium">{benefit}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Hosts Section -->
  <section class="mx-auto max-w-6xl px-4 py-12 sm:py-16 bg-white">
    <div class="text-center mb-12">
      <h2 class="text-3xl sm:text-4xl font-bold mb-4">Your Event Hosts</h2>
      <p class="text-xl text-slate-700 max-w-3xl mx-auto">
        Tim and Beth Byrd bring over 15 years of combined experience in financial planning and estate strategy to help you make informed decisions.
      </p>
    </div>

    <div class="bg-gradient-to-br from-brand-50 to-white rounded-2xl border border-brand-200 p-8 sm:p-12">
      <div class="grid lg:grid-cols-2 gap-8 items-center">
        <div>
          <h3 class="text-2xl font-bold mb-4">Tim & Beth Byrd</h3>
          <div class="space-y-4 text-slate-700">
            <p class="text-lg leading-relaxed">
              {site.team.intro}
            </p>
            <div class="grid sm:grid-cols-2 gap-4 pt-4">
              <div class="flex items-center space-x-2">
                <span class="w-2 h-2 bg-brand-500 rounded-full"></span>
                <span class="text-sm font-medium">$75M+ Assets Managed</span>
              </div>
              <div class="flex items-center space-x-2">
                <span class="w-2 h-2 bg-brand-500 rounded-full"></span>
                <span class="text-sm font-medium">300+ Policies Sold</span>
              </div>
              <div class="flex items-center space-x-2">
                <span class="w-2 h-2 bg-brand-500 rounded-full"></span>
                <span class="text-sm font-medium">Licensed in Georgia</span>
              </div>
              <div class="flex items-center space-x-2">
                <span class="w-2 h-2 bg-brand-500 rounded-full"></span>
                <span class="text-sm font-medium">20+ Years Community Service</span>
              </div>
            </div>
          </div>
        </div>
        <div class="text-center lg:text-right">
          <div class="inline-block bg-white rounded-xl p-4 shadow-lg">
            <div class="w-48 h-48 bg-slate-200 rounded-lg flex items-center justify-center mx-auto">
              <div class="text-slate-500 text-center">
                <svg class="w-16 h-16 mx-auto mb-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path>
                </svg>
                <p class="text-sm">Team Photo</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- RSVP Section -->
  <EventRSVP />

  <!-- Contact Information -->
  <section class="mx-auto max-w-4xl px-4 py-12 text-center">
    <div class="bg-amber-50 border border-amber-200 rounded-lg p-6">
      <h3 class="text-lg font-semibold text-amber-900 mb-2">Questions About the Event?</h3>
      <p class="text-amber-800 mb-4">
        Contact Tim and Beth directly for more information about this educational seminar.
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a 
          href={`tel:${site.phone}`} 
          class="inline-flex items-center justify-center gap-2 text-amber-900 hover:text-amber-700 font-medium"
        >
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path>
          </svg>
          {site.phone}
        </a>
        <a 
          href={`mailto:${site.email}`} 
          class="inline-flex items-center justify-center gap-2 text-amber-900 hover:text-amber-700 font-medium"
        >
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
          </svg>
          {site.email}
        </a>
      </div>
    </div>
  </section>

  <Footer />
</Base>

<script>
  // Smooth scrolling for anchor links
  document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
      e.preventDefault();
      const target = document.querySelector(this.getAttribute('href'));
      if (target) {
        target.scrollIntoView({ behavior: 'smooth' });
      }
    });
  });

  // Track event page view
  if (typeof gtag !== 'undefined') {
    gtag('event', 'page_view', {
      'event_category': 'engagement',
      'event_label': 'wills_trusts_event_page'
    });
  }
</script>